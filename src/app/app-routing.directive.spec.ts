import { AppRoutingDirective } from './app-routing.directive';

describe('AppRoutingDirective', () => {
  it('should create an instance', () => {
    const directive = new AppRoutingDirective();
    expect(directive).toBeTruthy();
  });
});
